[project]
name = "golden-fetcher"
version = "0.1.0"
description = "Golden BC inversion data fetcher"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32",
    "pandas>=2.0",
    "numpy>=1.26",
]
